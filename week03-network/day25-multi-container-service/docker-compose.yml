version: "3.8"

services:
  app:
    image: nginx
    container_name: my-app
    ports:
      - "8080:80"

  db:
    image: mysql:5.7
    container_name: my-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: testdb
